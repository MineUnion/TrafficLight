name: TrafficLight CI/CD

# 触发条件：push 到任意分支、手动触发
on:
  push:
    branches: [ '*' ] # 匹配所有分支
    tags: [ 'v*' ]    # 版本标签触发
  workflow_dispatch: # 手动触发

jobs:
  # 构建校验任务
  build:
    runs-on: ubuntu-latest
    outputs:
      should_release: ${{ steps.check_commit.outputs.should_release }}
      version: ${{ steps.extract_version.outputs.version }}
      display_version: ${{ steps.extract_version.outputs.display_version }} # 新增：用于展示的版本（含build号）
      jar_name: ${{ steps.build.outputs.jar_name }}

    steps:
      # 1. 检查代码（拉取完整历史）
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 2. 检查Commit信息，判断构建/发布逻辑
      - name: Check commit message
        id: check_commit
        run: |
          # 获取最新commit信息（容错：兼容手动触发/PR场景）
          COMMIT_MSG=$(git log -1 --pretty=format:"%s" 2>/dev/null || echo "manual trigger")
          echo "Latest commit message: $COMMIT_MSG"
          
          # 检测[miss build]，终止构建
          if echo "$COMMIT_MSG" | grep -qi "\[miss build\]"; then
            echo "❌ Commit contains [miss build], aborting build..."
            echo "should_build=false" >> $GITHUB_OUTPUT
            echo "should_release=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          # 检测[release]，标记发布
          if echo "$COMMIT_MSG" | grep -qi "\[release\]"; then
            echo "✅ Commit contains [release], will create Release later"
            echo "should_release=true" >> $GITHUB_OUTPUT
          else
            echo "ℹ️ No [release] in commit, skip Release creation"
            echo "should_release=false" >> $GITHUB_OUTPUT
          fi
          
          echo "should_build=true" >> $GITHUB_OUTPUT
        shell: bash

      # 3. 跳过构建（如果标记为不构建）
      - name: Skip build if needed
        if: steps.check_commit.outputs.should_build == 'false'
        run: |
          echo "Build aborted due to [miss build] in commit"
          exit 0
        shell: bash

      # 4. 配置JDK 17（自动缓存，无需手动指定JAVA_HOME）
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven # 自动缓存Maven依赖

      # 5. 提取版本号（核心修改：修复mvn命令嵌套语法错误）
      - name: Extract version
        id: extract_version
        run: |
          set -euo pipefail # 严格模式：任意错误立即终止
          
          # 步骤1：从pom.xml提取完整版本（含SNAPSHOT）
          FULL_VERSION=$(
            mvn help:evaluate -Dexpression=project.version -q -DforceStdout \
            -Dmaven.wagon.http.ssl.insecure=true \
            -Dmaven.wagon.http.ssl.allowall=true \
            -Dmaven.wagon.http.ssl.ignore.validity.dates=true 2>/dev/null || echo "1.0.0"
          )
          
          # 容错：空版本处理
          if [ -z "$FULL_VERSION" ] || [ "$FULL_VERSION" = "null" ]; then
            FULL_VERSION="1.0.0"
            echo "⚠️ 从pom.xml提取版本失败，使用默认版本：$FULL_VERSION"
          fi
          echo "完整版本（含SNAPSHOT）：$FULL_VERSION"
          
          # 步骤2：提取基础版本（移除SNAPSHOT后缀）
          BASE_VERSION=$(echo "$FULL_VERSION" | sed 's/-SNAPSHOT//g')
          echo "基础版本号：$BASE_VERSION"
          
          # 步骤3：获取GitHub Actions构建号（每次触发自增，全局唯一）
          BUILD_NUMBER=${{ github.run_number }}
          echo "构建号：$BUILD_NUMBER"
          
          # 步骤4：拼接展示版本（核心格式：TrafficLight 1.0.2 build XXX）
          DISPLAY_VERSION="TrafficLight $BASE_VERSION build $BUILD_NUMBER"
          
          # 步骤5：快照版追加分支+提交哈希（开发环境标识）
          if echo "$FULL_VERSION" | grep -qi "SNAPSHOT"; then
            # 提交哈希（容错）
            COMMIT_HASH=$(git rev-parse --short HEAD 2>/dev/null || echo "unknown")
            # 分支名（兼容所有触发场景，特殊字符替换）
            BRANCH_NAME=$(echo "${{ github.ref_name }}" | sed 's/[^a-zA-Z0-9_\-]/-/g' | cut -c1-30)
            BRANCH_NAME=${BRANCH_NAME:-"unknown-branch"} # 空值容错
            # 追加开发信息
            DISPLAY_VERSION="$DISPLAY_VERSION - $BRANCH_NAME - $COMMIT_HASH"
          fi
          
          # 输出最终版本（version：供文件名使用；display_version：供展示/Release使用）
          echo "version=$BASE_VERSION-build-$BUILD_NUMBER" >> $GITHUB_OUTPUT
          echo "display_version=$DISPLAY_VERSION" >> $GITHUB_OUTPUT
          echo "✅ 最终展示版本：$DISPLAY_VERSION"
          echo "✅ 产物版本标识：$BASE_VERSION-build-$BUILD_NUMBER"
        shell: bash

      # 6. 构建Maven项目（增强容错+产物校验）
      - name: Build with Maven
        id: build
        run: |
          set -euo pipefail
          
          # 执行Maven打包（强制刷新快照+SSL容错）
          mvn clean package -B \
            -Dmaven.wagon.http.ssl.insecure=true \
            -Dmaven.wagon.http.ssl.allowall=true \
            -Dmaven.wagon.http.ssl.ignore.validity.dates=true \
            -U # 强制更新快照依赖
          
          # 校验target目录是否存在
          if [ ! -d "target" ]; then
            echo "❌ target目录不存在，构建失败"
            exit 1
          fi
          
          # 获取Jar包名称（重命名为：基础版本-build-构建号.jar，便于识别）
          BASE_VERSION=$(echo "${{ steps.extract_version.outputs.version }}" | cut -d'-' -f1)
          ORIGINAL_JAR=$(ls target/*.jar 2>/dev/null | grep -v "original-" | head -n1 || true)
          
          if [ -z "$ORIGINAL_JAR" ]; then
            echo "⚠️ 未找到构建的Jar包，使用默认名称"
            JAR_NAME="TrafficLight-$BASE_VERSION-build-${{ github.run_number }}.jar"
          else
            # 重命名Jar包（含版本+构建号）
            JAR_NAME="TrafficLight-$BASE_VERSION-build-${{ github.run_number }}.jar"
            cp "$ORIGINAL_JAR" "target/$JAR_NAME"
          fi
          
          echo "jar_name=$JAR_NAME" >> $GITHUB_OUTPUT
          echo "✅ 构建产物：$JAR_NAME"
          
          # 复制产物到统一目录（容错）
          mkdir -p build-output
          if [ -f "target/$JAR_NAME" ]; then
            cp "target/$JAR_NAME" build-output/
            echo "✅ 复制Jar包到build-output目录"
          else
            echo "⚠️ Jar包不存在，跳过复制"
            touch build-output/empty.txt # 避免上传为空
          fi
          
          # 列出产物目录
          ls -la build-output/
        shell: bash

      # 7. 上传构建产物（容错：允许空目录）
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.extract_version.outputs.display_version }}
          path: build-output/
          retention-days: 7
          if-no-files-found: warn # 无文件时仅警告，不终止

  # 自动创建Release任务（仅当标记为需要Release时执行）
  release:
    needs: build
    runs-on: ubuntu-latest
    if: needs.build.outputs.should_release == 'true'
    steps:
      # 1. 下载构建产物
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.build.outputs.display_version }}
          path: build-output

      # 2. 校验产物是否存在
      - name: Validate build artifact
        run: |
          set -euo pipefail
          if [ ! -f "build-output/${{ needs.build.outputs.jar_name }}" ]; then
            echo "❌ 构建产物不存在，发布失败"
            exit 1
          fi
          ls -la build-output/
        shell: bash

      # 3. 创建GitHub Release（使用含build号的展示版本）
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          # 标准化标签（含build号，避免重复）
          tag_name: v${{ needs.build.outputs.version }}
          # Release名称（核心格式：TrafficLight 1.0.2 build XXX）
          name: ${{ needs.build.outputs.display_version }}
          # Release正文（兼容无提交信息场景）
          body: |
            ## 🚀 ${{ needs.build.outputs.display_version }}
            ### 分支：${{ github.ref_name }} | 构建时间：${{ github.run_at }}
            ### 构建号：${{ github.run_number }} | 提交哈希：${{ github.sha }}
            ${{ github.event.head_commit.message && format('### 提交信息：{0}', github.event.head_commit.message) || '### 触发方式：手动构建' }}
            
            #### 构建说明
            - 基于 `${{ github.ref_name }}` 分支构建
            - 产物：${{ needs.build.outputs.jar_name }}
            - 版本格式：主版本.次版本.修订版 build 构建号（构建号全局自增）
          # 上传产物（绝对路径容错）
          files: ${{ github.workspace }}/build-output/${{ needs.build.outputs.jar_name }}
          # 预发布标记（兼容main/master分支）
          prerelease: ${{ github.ref_name != 'main' && github.ref_name != 'master' }}
          # 允许覆盖已有标签（可选）
          overwrite: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
